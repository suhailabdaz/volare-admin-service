export class Banner {
  constructor(
    public readonly banner_content: string,
    public readonly _id: string,
    public readonly banner_image_link:string,
    public readonly status : boolean
  ) {}
}
